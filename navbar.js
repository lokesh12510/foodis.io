lled = () => {
    dec = 10;
    ec
}
ScrollTop = 0;
ar = document.getElementById("navbar");
ddEventListener("scroll", function() {
            navbar.style.setProperty('background', scrolled() > 50 ? "#000000" : "#asasas")
            var scrollTop = window.pageYOffset || document.documentElement.scrollTop;
            llTop > lastScrollTop) {
            ar.style.top = "-100px";

        } else {
            ar.style.top = "0";

        }
        lastScrllTop = scrollTop;